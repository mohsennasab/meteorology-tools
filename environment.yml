# Conda environment for local use and for mybinder.org (repo2docker auto-detects this file).
# Versions are left unpinned so Binder can solve quickly; the stack below was developed and
# tested with the versions noted in the README ("Tested with").
name: meteorology-tools
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy
  - pandas
  - scipy
  - geopandas>=1.0.1
  - shapely>=2.1.2
  - geos
  - proj
  - proj-data
  - rasterio
  - pyproj
  - matplotlib
  - matplotlib-scalebar
  - contextily
  - mapclassify
  - jupyterlab
  - notebook
  - dask
  - fiona
  - ipykernel
  - pip
  - pyarrow
  - pytest
  - requests
  - rioxarray
  - ruff
  - xarray
  - zarr
  - pip:
    - pystac
    - dataretrieval
    - duckdb
    - nbconvert
    - boto3
    - leafmap
    - netcdf4
    - s3fs
    - hecdss
    - python-dotenv
    - stac_geoparquet
    - stormhub
